#include <random>
#include <limits>
#include <stack>
#include <iomanip>
#include <algorithm>

#include "Random.h"
#include "Date.h"
#include "helpers.h"

#include "Person.h"

#include "Seller.h" 
#include "Buyer.h"
#include "Realtor.h"
#include "Agency.h"

using std::cout;
using std::endl;

int main() {	 
	setRus();
/* 
	cout << Person::generate() << endl;
	cout << Person::generate(); */


	/* for (int i = 0; i < 5; i++) {
		cout << Random::getInt(10, 99) << endl;
	} 
 */
 


	/* cout << Person::generate() << "\n\n";
	cout << Person::generate();  */

	// cout << '"' << trim(" a ") << '"';

	/* print<std::string>({
		trim("  a  "), 
		"lol"
	}); */
	
	return 0; 
}

  